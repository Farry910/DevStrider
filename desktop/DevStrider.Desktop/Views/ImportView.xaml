<UserControl x:Class="DevStrider.Desktop.Views.ImportView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <DockPanel>
    <Grid DockPanel.Dock="Top" Margin="0,0,0,8">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="Auto"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Column="0" Text="Import peer snapshots" Style="{StaticResource H1}" VerticalAlignment="Center"/>
      <TextBlock Grid.Column="1" Text="{Binding StatusMessage}" VerticalAlignment="Center" Margin="16,0" Foreground="{StaticResource Muted}"/>
      <Button Grid.Column="2"
              Command="{Binding LoadCommand}"
              Style="{StaticResource IconButton}"
              ToolTip="Refresh repo"
              Content="&#xE72C;"/>
    </Grid>

    <Grid>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="180"/>
        <ColumnDefinition Width="2*"/>
        <ColumnDefinition Width="2*"/>
      </Grid.ColumnDefinitions>

      <!-- Days in the repo -->
      <Border Grid.Column="0" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="6">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="Day folders" FontWeight="SemiBold" Margin="2,2,2,6"/>
          <ListBox ItemsSource="{Binding AvailableDays}"
                   SelectedItem="{Binding SelectedDay, Mode=TwoWay}"
                   DisplayMemberPath="."/>
        </DockPanel>
      </Border>

      <!-- Files on the selected day -->
      <Border Grid.Column="1" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="6" Margin="8,0">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="Peers on the selected day" FontWeight="SemiBold" Margin="2,2,2,6"/>
          <DataGrid ItemsSource="{Binding AvailableFiles}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="*"/>
              <DataGridTextColumn Header="Path"  Binding="{Binding Path}" Width="2*"/>
              <DataGridTemplateColumn Header="Import" Width="56">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Command="{Binding DataContext.ImportFileCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding ., FallbackValue={x:Null}, TargetNullValue={x:Null}}"
                            Style="{StaticResource IconButton}"
                            ToolTip="Import snapshot"
                            Content="&#xE896;"/>
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>

      <!-- Locally imported snapshots -->
      <Border Grid.Column="2" Background="{StaticResource Surface}" BorderBrush="{StaticResource Border}" BorderThickness="1" Padding="6">
        <DockPanel>
          <TextBlock DockPanel.Dock="Top" Text="Imported locally" FontWeight="SemiBold" Margin="2,2,2,6"/>
          <DataGrid ItemsSource="{Binding LocalSnapshots}">
            <DataGrid.Columns>
              <DataGridTextColumn Header="Owner"   Binding="{Binding Owner}" Width="*"/>
              <DataGridTextColumn Header="Day"     Binding="{Binding DayKey}" Width="100"/>
              <DataGridTextColumn Header="Imported" Binding="{Binding ImportedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" Width="160"/>
              <DataGridTemplateColumn Header="Remove" Width="56">
                <DataGridTemplateColumn.CellTemplate>
                  <DataTemplate>
                    <Button Command="{Binding DataContext.DeleteSnapshotCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                            CommandParameter="{Binding ., FallbackValue={x:Null}, TargetNullValue={x:Null}}"
                            Style="{StaticResource DangerIconButton}"
                            ToolTip="Remove local copy"
                            Content="&#xE74D;"/>
                  </DataTemplate>
                </DataGridTemplateColumn.CellTemplate>
              </DataGridTemplateColumn>
            </DataGrid.Columns>
          </DataGrid>
        </DockPanel>
      </Border>
    </Grid>
  </DockPanel>
</UserControl>
